#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @a - First integer
 * @b - Second integer
 * Return: always 0
 */

int mul(int a, int b)
{
int result = a * b;
return (result);
}
